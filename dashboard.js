const h=getHistory();const n=k=>h.filter(x=>x.result===k).length;document.getElementById("total").textContent=h.length;document.getElementById("suspicious").textContent=n("suspicious");document.getElementById("low").textContent=n("low");document.getElementById("unable").textContent=n("unable");const vals=[n("suspicious"),n("low"),n("unable")], labels=["Suspicious","Low Risk","Unable"];const max=Math.max(1,...vals);document.getElementById("bars").innerHTML=vals.map((v,i)=>`<div class="bar-wrap"><b>${v}</b><div class="bar" style="height:${Math.max(8,v/max*190)}px"></div><span>${labels[i]}</span></div>`).join("");document.getElementById("activity").innerHTML=h.length?h.slice(0,7).map(x=>`<div class="activity-row"><span>${x.result==="suspicious"?"High-Risk / Suspicious":x.result==="low"?"Low Risk":"Unable to Analyze"}</span><span>${formatDate(x.date)}</span></div>`).join(""):`<div class="empty">No screening history yet.</div>`;